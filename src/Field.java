public class Field {
    double x_length;
    double y_length;
    Obstacle[] obstacles;

    Field(double x_length, double y_length) {
        this.x_length = x_length;
        this.y_length = y_length;
    }

}
